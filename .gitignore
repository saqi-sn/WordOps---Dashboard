# Backend secrets — never commit
backend/config.php

# Frontend build + deps
frontend/dist/
frontend/*.tsbuildinfo
node_modules/

# Env files
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
*.log

# internal build plan / notes — not public
CLAUDE.md

# runtime settings store (admin creds, S3) — never commit
backend/data/
